config { type: "assertion", tags: ["gold", "assertion", "adherence"] }

SELECT userId, reference_month, adherence_score, adherence_label
FROM ${ref("gold_user_adherence")}
WHERE
  userId             IS NULL
  OR reference_month IS NULL
  OR adherence_score IS NULL
  OR adherence_label IS NULL
  OR planId          IS NULL